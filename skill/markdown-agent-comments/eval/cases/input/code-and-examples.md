# Trigger Examples

Use `@codex` in docs when explaining the trigger syntax.

```md
@agent example only
> [!NOTE] example callout
>
> @claude example only
```

> @claude copied terminal output
> npm error code EOTP

The sync can break your vault if something goes wrong. @codex make this warning calmer and more precise.
