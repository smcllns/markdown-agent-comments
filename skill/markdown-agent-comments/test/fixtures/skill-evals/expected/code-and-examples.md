# Trigger Examples

Use `@codex` in docs when explaining the trigger syntax.

```md
@agent example only
> [!NOTE] example callout
>
> @claude example only
```

> @claude copied terminal output
> npm error code EOTP

Sync changes are applied in place, so review the diff before you keep the result.

> [!DONE]- clarified sync warning
>
> [@user] The sync can break your vault if something goes wrong. @codex make this warning calmer and more precise.
>
> [@codex] done - made the warning calmer and more precise. <!--mdac:eot-->
